# Server

## Step 1 create package json file
```bash
npm init -y
```

## step  2 install package
```bash
npm install express nodemon cors morgan bcryptjs jsonwebtoken zod prisma
```

## step3 Git
```bash
git init
git add .
git commit -m "message"
```

next Step
copy code from repo
only first time
```bash remote add origin https://github.com/chatcharin5360/cc19-front-to-back-api.git
get branch -m main
get push -m origin main

```





